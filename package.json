{
  "name": "immutable-partition",
  "version": "0.0.0",
  "description": "A partition helper returning ImmutableJS structures",
  "main": "lib/index.js",
  "scripts": {
    "lint": "eslint .",
    "test:watch": "npm run test -- --watch",
    "test": "jest",
    "prepublish": "npm run lint && npm run test && npm run build",
    "build": "babel index.js --out-dir lib"
  },
  "keywords": [
    "immutable",
    "immutablejs",
    "partition",
    "split"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/ArnaudRinquin/immutable-partition.git"
  },
  "author": "Arnaud Rinquin <rinquin.arnaud@gmail.com> (http://github.com/ArnaudRinquin)",
  "license": "MIT",
  "peerDependencies": {
    "immutable": "^3.0.0"
  },
  "devDependencies": {
    "babel-jest": "^18.0.0",
    "babel-preset-es2015": "^6.18.0",
    "immutable": "^3.0.1",
    "jest": "^18.0.0"
  },
  "dependencies": {
    "babel-cli": "^6.18.0",
    "eslint": "^3.12.2"
  }
}
